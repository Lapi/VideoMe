library VideoMeController;

import 'dart:json';

class VideoMeController {

  getCarousellItems(){
    var encoded = JSON.stringify({'name': 'getCarousellItems'});
//    _sendEncodedMessage(encoded);
  }  
}
